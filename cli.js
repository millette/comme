'use strict'

const run = require('.')

const iii = require('./jido2018.json').features // .slice(0, 50)

run(iii)

/*
const foo = new Booya(input)
foo.doit()
const b = foo.prepareDisplay()
console.log(JSON.stringify(b, null, '  '))
*/
